import React from 'react'

function Functiondemo(props) {
    
  return (
    <div>
        <center><h1>Hello {props.name} with id {props.id}</h1></center>
        <center>function demo </center>
    </div>
  )
}

export default Functiondemo;